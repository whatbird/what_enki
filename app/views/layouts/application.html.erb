<!DOCTYPE html>
<html dir="ltr" lang="en-US">
<head>
  <meta charset="utf-8">
  <title><%= html_title yield(:page_title) %></title>
  <%= csrf_meta_tag %>
  <%= stylesheet_link_tag 'log55' %>
  <%= javascript_include_tag 'jquery' %>
  <%= javascript_include_tag 'rails' %>
  <%= open_id_delegation_link_tags(enki_config[:open_id_delegation, :server], enki_config[:open_id_delegation, :delegate]) if enki_config[:open_id_delegation] %>
  <%= yield(:head) %>
</head>
<body>
  <div id="page_container">

    <div id="main">
      <%= yield %>
    </div>

    <div id="navigation">
      <h1 id="page_heading">
        <a href="/">
          <%= image_tag('55_log_logo.gif', :alt => config[:title] ) %>
        </a>
      </h1>
      <!-- <h2>Pages</h2> -->
      <% if false %>
      <ul>
        <% page_links_for_navigation.each do |link| -%>
          <li><%= link_to(h(link.name), link.url) %></li>
        <% end -%>
      </ul>
      <% unless category_links_for_navigation.empty? -%>
        <h2>tags</h2>
        <% if 1  %>
          <%#= tag_cloud_nav %>
        <% else %>
        <ul>
          <% page_links_for_navigation.each do |link| -%>
            <li><%= link_to(link.name, link.url) %></li>
          <% end -%>
        </ul>

        <% end -%>
      <% end -%>

      <form method="get" id="sform" action="http://www.google.com/search">
        <div>
          <h2><label for="q">Search</label></h2>

          <input type="text" id="q" value="" name="q" />
          <input type="hidden" name="sitesearch" value="<%=h config[:url] %>" />
        </div>
      </form>
      <% end %>
    </div>    
    
    <div id="footer">
      Valid <a href="http://validator.w3.org/check?uri=referer">XHTML</a> and <%= link_to "ATOM", "http://feedvalidator.org/check.cgi?url=#{h(config[:url])}/posts.atom" %>. <a href="http://www.enkiblog.com" title="A Ruby on Rails blogging app for the fashionable developer">Enki</a>.</div>
  </div>
  
  <% if RAILS_ENV=='production' %>

  <script type="text/javascript">
  var gaJsHost = (("https:" == document.location.protocol) ? "https://ssl." : "http://www.");
  document.write(unescape("%3Cscript src='" + gaJsHost + "google-analytics.com/ga.js' type='text/javascript'%3E%3C/script%3E"));
  </script>
  <script type="text/javascript">
  try {
  var pageTracker = _gat._getTracker("UA-7600517-4");
  pageTracker._trackPageview();
  } catch(err) {}</script>
  <% end %>
  
</body>
</html>
